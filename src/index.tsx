import * as React from 'react';
import * as ReactDOM from 'react-dom';
import App from '@/App';

/**
 * Use element with id "app" as container
 */
ReactDOM.render(
  <App />,
  document.getElementById('app'),
);
